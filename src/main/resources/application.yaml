spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo?characterEncoding=utf-8&SSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  devtools:
    restart:
      enabled: true
      exclude: src/main/java/
server:
  port: 8088
